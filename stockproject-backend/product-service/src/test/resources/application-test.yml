spring:
  data:
    mongodb:
      database: test
      uri: mongodb://localhost:27017/test
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

logging:
  level:
    org.springframework.data.mongodb: DEBUG
    com.mongodb: DEBUG